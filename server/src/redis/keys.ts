export const roomStateKey = (code: string) => `room:${code}:state`;
export const roomHistoryKey = (code: string) => `room:${code}:history`;
export const snapshotKey = (id: string) => `snapshot:${id}`;